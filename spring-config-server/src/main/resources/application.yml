# Server Port for boot server
server:
  port: 8888

# spring cloud config server git uri
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/vikramgulia/spring-config-repo.git

# Security enable/disable
security:
  basic:
    enabled: false